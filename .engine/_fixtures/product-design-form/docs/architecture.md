# Architecture

## Overview and context

A negative fixture: this architecture overview carries every required section but leaves out the diagram, so
the design-form gate must bite at hard severity.

## The main parts

The main parts would be shown here as a simple diagram — deliberately omitted so the check fires.

## How it behaves at runtime

It does not; it is a fixture.

## Key decisions

To omit the diagram, so the gate is witnessed catching a real bad input.
