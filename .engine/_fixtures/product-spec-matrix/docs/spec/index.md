# Product spec (negative fixture — NOT a real product spec)

Seeds a settled capability so the obligation-matrix derivation is non-empty, so the drift gate has real
rows to compare the deliberately-stale committed matrix (`../stale-matrix.json`) against and MUST bite.

| Capability | Status | Doc |
| --- | --- | --- |
| A | settled | [A](a.md) |
