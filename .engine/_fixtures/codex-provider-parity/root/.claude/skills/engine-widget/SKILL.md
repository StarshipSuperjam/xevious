---
name: engine-widget
description: A deliberately one-sided fixture — a Claude command with no Codex twin and no ledger entry.
invocation: operator-typed
disable-model-invocation: true
---

## Steps

1. Exists only on the Claude side, so the parity guard must go red.
