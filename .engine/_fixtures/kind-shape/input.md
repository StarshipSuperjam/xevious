## Status

A deliberately-broken example: the required sections are all present but in the WRONG order
(Status before Decision/Rationale), so the shape check must catch the ordering.

## Decision

The decision section appears second here, though the template requires it first.

## Rationale

The rationale section appears last in the required order, but here it trails Decision correctly —
the violation the shape check should flag is that Status leads.
