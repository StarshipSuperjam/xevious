---
name: engine-bad
description: A deliberately broken fixture — an engine Codex command shipped WITHOUT its operator-only policy companion.
---

## Steps

1. This render has no agents/openai.yaml, so the guard must go red.
