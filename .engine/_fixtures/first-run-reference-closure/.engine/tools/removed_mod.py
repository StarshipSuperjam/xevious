# A stand-in for a first-run asset the engine removes at setup. It is listed in this mini-tree's
# first-run-assets.json and is present on disk here, which clears the check's "machinery already
# removed" no-op so the survivor reference below is actually evaluated.
VALUE = "removed first-run helper"
