---
required_sections: ["Steps"]
allowed_sections: ["Notes"]
length_budget: 60
---

<!-- A Codex skill under .agents/skills/ is a COMMITTED RENDER: never author one from this template
by hand — author or edit its canonical Claude twin (.claude/skills/<name>/SKILL.md, the skill.md
template) and run `uv run --directory .engine --frozen -- python tools/codex_gen.py generate`.
This template exists so the render's SHAPE is checked exactly like every prose surface (the same
Steps/Notes contract as the Claude twin); the render tool produces this shape by construction. -->

## Steps

<The ordered steps, rendered from the canonical Claude twin — the same procedure, with the typed
prefix in Codex's own form ($engine-… instead of /engine-…).>

## Notes

<Optional, rendered from the canonical Claude twin.>
