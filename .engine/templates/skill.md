---
required_sections: ["Steps"]
allowed_sections: ["Notes"]
length_budget: 60
---

## Steps

<The ordered steps to perform this skill, numbered, one action per step, written so the model or operator can follow them in-session without guessing. Keep the skill thin: if the procedure has real depth, do not restate it here — make the step "follow the operation at <path>" so there is one home for the procedure. Name the exact command, file, or action each step takes.>

## Notes

<Optional. Anything that helps run the skill safely but is not a step — when to reach for it, common failure points and how to recover, or what it deliberately does not do. Delete this whole section if there is nothing to add.>
