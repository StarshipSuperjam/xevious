# Understanding <the topic>

<!-- Starting shape for an understanding-oriented explanation under docs/explanation/. An explanation gives the
background and reasoning that make the rest make sense — it is read to understand, away from the keyboard, not
followed step by step. Copy it to docs/explanation/<topic>.md. This is one of four kinds of user guide (learning
tutorial, task how-to, reference, explanation); these guides are optional, so author only the kinds the product
needs. A guide's SHAPE is checked when it is present (the engine confirms the sections a guide of its kind
needs), but whether it is RIGHT is yours to get right — the engine never judges that. It is the place for context, history,
trade-offs, and the why behind a decision — deliberately not instructions (that is the tutorial and how-to) and
not a fact table (that is the reference). Strip these comments and the bracketed placeholders before committing. -->

## The question this answers

<One or two sentences naming what a reader is trying to understand — the confusion this document clears up.>

## Background

<The context, history, or ideas the reader needs to make sense of this part of the product. Discuss; do not
instruct.>

## Why it works this way

<The reasoning and the trade-offs — what was weighed, what was chosen, and what that costs — so a reader
understands the shape of the product, not just its surface.>
