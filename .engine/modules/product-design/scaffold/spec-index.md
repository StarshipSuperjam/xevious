---
spec_depth: full
---

# Product spec

<!-- Starting shape for the master index at docs/spec/index.md — the one place that lists every capability,
its stage, and a link to its document, so the spec holds together as a whole. Copy it to docs/spec/index.md
and keep one row per capability. Every capability document must appear here, and every link here must point to
a document that exists. The Status column may read either the plain stage ("not yet described" / "in progress"
/ "settled") or the short marker the document carries — but it must match that document's own stage. A piece
you have not described yet still gets a row and a short document marked not-yet-described; fill it in later.
The example row below points at the capability template that ships beside this file; replace it with your real
capabilities. (The scaffold conformance test mirrors this example link target, so if you rename it, update
that test too.)

The frontmatter `spec_depth` records how much to write up — the operator's own choice, set during the intake,
NOT invented by you: `full` (the default — also write the guiding principles and the architecture overview) or
`light` (the recorded opt-out — the description under docs/spec/ only). Keep it `full` unless the operator
chose to keep this description light, in which case set it to `light`. When it is `full`, the design-form
check expects docs/principles.md and docs/architecture.md; when it is `light`, those are not expected. This
line stays in the committed file (unlike the guidance comments, which are stripped). -->

| Capability | Status | Doc |
| --- | --- | --- |
| Capability name | in progress | [Capability name](spec-capability.md) |
