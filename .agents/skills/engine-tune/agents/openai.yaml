# GENERATED by .engine/tools/codex_gen.py — this engine command is operator-typed:
# the model must never invoke it on its own; the operator's explicit $-invocation still works.
policy:
  allow_implicit_invocation: false
